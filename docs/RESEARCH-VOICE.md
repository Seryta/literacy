# 语音能力选型调研

## 当前版本语音目标

语音能力的目标是支撑识字教学闭环，而不是做口语考试系统。

当前版本需要：

- 本地 STT：把用户说的话转成文本
- 本地 TTS：让 AI 老师用标准普通话说话
- Android 可落地
- 对中老年用户尽量清晰、自然

这里的“本地 STT/TTS”只表示语音推理在设备端完成；识别结果仍会进入在线 LLM 的教学 turn，不代表产品支持离线教学。

当前版本不需要：

- 精细发音评分
- 声调级纠错
- 方言识别和适配

## 语音在产品中的角色

### STT

用于：
- 听懂用户回答
- 理解用户是否认得字
- 获取用户的解释和造句
- 接收用户的界面控制语音，如“显示拼音”“再来一遍”

### TTS

用于：
- 老师语音讲解
- 单字或词语示范
- 简短反馈与鼓励

### 不做发音评分

当前版本是识字软件，不把“你读得准不准”作为主要教学目标。
只要系统能听懂用户表达，并能继续教学，就已经满足主线需求。

## STT（语音识别 -> 文本）

| 方案 | 中文效果 | 模型大小 | Android 适配 | 备注 |
|------|---------|---------|-------------|------|
| **SenseVoice-small** | 优秀 | ~94MB | 需封装 | 中文表现强 |
| **Whisper.cpp tiny/small** | 较好 | 78MB / 466MB | 有 Java 绑定 | 社区成熟 |
| **Sherpa-ONNX** | 好 | 多种可选 | 有 Android 示例 | 适合统一方案 |

### 当前候选

当前更值得优先验证的候选：
- **SenseVoice-small**
- **Sherpa-ONNX**
- **Whisper.cpp tiny**

选择标准：
- 标准普通话识别效果
- 低端 Android 设备延迟
- 内存占用
- 集成工作量

## TTS（文本 -> 语音）

| 方案 | 中文效果 | 延迟 | Android 适配 | 备注 |
|------|---------|------|-------------|------|
| **CosyVoice 2** | 优秀 | 中 | 需 ONNX 导出 | 音质最佳但集成偏重 |
| **Sherpa-ONNX VITS** | 较好 | 低 | 有 Android 示例 | 第一阶段较务实 |
| **MeloTTS** | 较好 | 低 | 需转换 | 轻量可选 |
| **Piper TTS** | 一般 | 低 | 有 C++ 库 | 中文自然度一般 |

### 当前候选

当前更像两档路线，而不是最终拍板：
- **Sherpa-ONNX VITS**：更偏首期落地和快速跑通
- **CosyVoice 2**：更偏后续音质升级候选

## 标准普通话约束

当前版本默认：

- 系统输出为标准普通话
- 用户输入按普通话场景理解
- 不对用户口音做分级或评分

这有两个好处：
- 产品范围更聚焦
- 避免把识字主线拖入语音评测难题

当 STT 置信度不足、背景噪声较大或没有识别到内容时：
- 先请用户重说或改用屏幕选项
- 不把识别失败记录为用户答错
- 连续识别失败时由 Agent 降低交互难度，而不是反复要求用户重来

## 对教学设计的影响

既然不做发音评分，语音链路的判断标准就变成：

1. 能否稳定听懂用户意图
2. 能否让老师语音自然清晰
3. 能否支撑 turn 级别的实时互动

因此，语音模块主要服务于：
- 对话连续性
- 教学节奏
- 可理解的示范

而不是服务于：
- 语音考试
- 口音纠错
- 音素级分析

## 当前倾向

| 模块 | 首选 | 备选 |
|------|------|------|
| STT | SenseVoice-small / Sherpa-ONNX / Whisper.cpp tiny | 待首版 POC 实测决策 |
| TTS | Sherpa-ONNX VITS / CosyVoice 2 | 待首版 POC 实测决策 |
| 发音评估 | 当前版本不做 | - |
| 统一引擎 | Sherpa-ONNX（若 STT/TTS 效果都可接受） | 待验证 |

## 语音体验标准（草案，待首版 POC 实测确认）

- **音色**：稳重、温和的自然人声。中老年用户对沉稳音色接受度更高（见 `RESEARCH-THEORIES.md` §9.4）。首版提供 1-2 个可选音色（如女声 / 男声各一）。
- **语速档位**：慢 / 正常 / 快 三档（TTS 倍速建议 0.9 / 1.0 / 1.1，待实测），建档时可选，用户可随时语音调整。
- **STT 连续失败降级**：
  - 连续 2 次识别失败 → 提示"请再说一遍"（本地，不触发 LLM）
  - 连续 3 次识别失败 → 产生 `RecognitionRepeatedFailures` 事件（触发 LLM，见 `AGENT-PROTOCOL.md` 事件表），Agent 降难或切换到屏幕选项（`show_options`）
  - 识别失败一律不记录为学习错误
- **turn 级验收指标**（待首版 POC 实测）：STT → LLM → TTS 单 turn 端到端时延、识别率、用户主观评分（音色 / 语速 / 清晰度）

## 待验证

- [ ] 不同 STT 方案的首版 POC 对比与选型结论
- [ ] 不同 TTS 方案的首版 POC 对比与选型结论
- [ ] SenseVoice 在 Android 上的延迟和内存占用
- [ ] Sherpa-ONNX 中文 TTS 的实际音质
- [ ] STT 与 TTS 在 turn 级交互中的切换时延
- [ ] 中老年用户对不同音色和语速的主观偏好
