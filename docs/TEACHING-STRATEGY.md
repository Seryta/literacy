# 教学策略矩阵

本文档定义 Agent 的教学行为规则，从"原则"升级到"可执行指令"。Agent 负责在允许的规则内调用工具，不做自由发挥。

---

## 1. 教学优先级机制

### 1.1 统一优先级模型

用一套统一的优先级机制替代"名字优先"硬编码：

```
priority_queue = [
  { type: "user_request",  chars: [...], reason: "用户主动要求" },
  { type: "name_plan",     chars: [...], reason: "姓名学习计划" },
  { type: "life_pack",     chars: [...], reason: "生活字包（当前进行中）" },
  { type: "review_due",    chars: [...], reason: "复习到期" },
  { type: "curriculum",    chars: [...], reason: "课程顺序" }
]
```

优先级规则：
- `user_request` 始终最高——用户说"我想学这个字"，立即响应
- 用户请求完成后，回到上一个未完成的 priority 项目
- `name_plan` 默认排在 `life_pack` 和 `curriculum` 之前，但不是硬门槛
- 用户可以通过语音随时重新排序（"先不学名字了，我想学药盒上的字"）

### 1.2 用户请求介入流程

```
用户："我想学'药'字"
  → Agent: 确认请求，标记为 user_request
  → 记录当前进度（当前在哪种类型的哪个字）
  → 切换到"药"字教学
  → 完成或用户表示学够了
  → 问"要不要继续学之前的？"（或自动回到上一个 priority 项）
```

---

## 2. 降难策略矩阵

从"提示强度"和"学习通道"两个维度定义降难规则：

### 2.1 提示强度等级

| 等级 | 名称 | 说明 | 屏幕表现 | 适用场景 |
|------|------|------|---------|---------|
| L0 | 无提示 | 用户完全独立 | 空白米字格 + 字的目标展示 | 独立书写 / 复习 |
| L1 | 最小提示 | 给一个关键锚点 | 第 1 笔起点闪烁 | 用户刚学完尝试独立写 |
| L2 | 部件提示 | 拆字提示 | 显示字拆成哪几部分 | 用户连续 2 次卡在笔画 |
| L3 | 笔画引导 | 逐笔展示 | 灰色笔画 + 高亮当前笔 | 跟写阶段 |
| L4 | 完整示范 | 先写一遍给用户看 | 完整笔画动画 | 用户表示完全不会写 |
| L5 | 拼音辅助 | 显示拼音 | 拼音标注在字上方 | 用户不知道字怎么读 |
| L6 | 跳过 | 允许跳过 | 确认跳过 | 用户明确表示不想学或太难 |

### 2.2 降难触发规则

```
连续 2 次在同一阶段卡住 → 降一级（L3→L4）
连续 2 次在最低提示等级仍卡住 → L5（拼音辅助）
L5 仍卡住 → L6（建议跳过，等下次或换个方式学）

成功完成（不需要提示） → 升一级（下次用更低提示）
```

"连续次数"由 `lesson_state` 在阶段内累计（内存态，不落库）；跨 session 只保持提示等级（`characters.current_prompt_level`），不保留卡住计数。

### 2.3 通道切换策略

当用户在某个学习通道卡住时，切换通道而非增加提示：

| 当前通道 | 卡住时切换到 |
|---------|------------|
| 书写（写不出） | 识别（"你从这个几个字里选出来"） |
| 识别（认不出） | 理解（"我给你看看它代表什么"→ 看图） |
| 理解（说不出） | 示范（"你看着老师先写一遍"） |
| 拼音（不会拼） | 整字语音（直接读整个字，不拆拼音） |

---

## 3. 教学路径矩阵：按学习者类型

### 3.1 路径类型

| 路径 | 适合人群 | 特征 | 策略 |
|------|---------|------|------|
| **识写并进** | 有书写意愿和能力 | 每字都跟写+独立写 | 默认路径 |
| **识主写辅** | 书写困难或非优先目标 | 认读为主，书写简化 | 独立写要求改为听音选字 |
| **识读优先** | 主要为阅读理解场景 | 认读+理解，书写不做要求 | 独立写阶段替换为选字填空 |

### 3.2 路径选择

- 首启建档时询问："您平时需要用笔写字吗？还是能认出字就行？"
- 默认规则：
  - 有书写意愿 → `write_parallel`（识写并进）
  - 书写困难 / 手抖 / 明确表示不想写字 → `read_primary`（识主写辅）
  - 主要阅读场景 → `read_only`（识读优先）
- 兜底：首启询问无明确答案时默认 `write_parallel`；首个独立书写阶段连续 2 次失败或用户表示手不方便 → 自动降级为 `read_primary`
- 路径写入 `profile.learning_path`，影响独立书写阶段的检测方式与成功条件（见 `AGENT-PROTOCOL.md` §6.3）
- 用户可在任何 session 中通过语音切换路径（"我今天手不太方便，不写字了"）
- 路径影响独立书写阶段的成功标准和练习类型

---

## 4. 脚手架撤除策略

从 L4（完整示范）→ L0（独立完成）的逐步撤除过程：

```
第 1 次练习：L4（完整示范，用户跟着写）
第 2 次练习：L3（逐笔引导，只在需要时高亮）
第 3 次练习：L2（部件提示，显示字的结构拆分）
第 4 次练习：L1（最小提示，只给起笔位置）
第 5 次练习：L0（独立书写）
```

每次挑战成功后：提高一级（减少提示）  
每次挑战失败后：降一级（增加提示），不重新从 L4 开始

---

## 5. 跨 session 状态保持

### 5.1 继续学习

新 session 启动时，Agent 通过 `session_brief` 获知：
- 上次学到哪个字的哪个阶段
- 上次离开时的提示等级
- 上次是在什么情绪状态下结束的（疲劳/主动/正常结束）

恢复策略：
- 如果上次在失败/疲劳中结束 → 降一级提示，从更简单的开始
- 如果上次成功完成 → 不提高难度，保持在同一等级，验证是否还记得
- 如果间隔超过 3 天 → 降一级提示，确保复习效果

### 5.2 复习 vs 新课决策

```
启动时先看 review_queue：
  - 如果 overdue 字数 ≥ 3 → 本次 session 以复习为主，2-3 个复习字
  - 如果 overdue 字数 < 3 → 复习 1-2 个 overdue，然后进入新课或继续上次
  - 如果无 overdue → 直接继续上次或进入新课
```

---

## 6. 激励策略

### 6.1 不使用

- 分数、排名、百分比
- 红叉、对错标记
- 考试式评价（"及格""不及格"）
- 儿童化奖章、星星、等级
- "你太棒了""真聪明"

### 6.2 使用

- 具体能力描述："你现在能认出这 5 个字了"
- 场景关联："下次去医院，你可以自己看挂号单了"
- 对比进步："上一次你写'国'字少了一横，这次写全了"
- 自然结尾："今天学了三个字，张、三、李"

---

## 7. 疲劳与暂停协议

| 信号 | Agent 响应 |
|------|-----------|
| "累了" | 立即停止当前教学，不尝试完成当前字 |
| "休息一下" | 暂停但不结束 session，记录进度，用户可随时回来 |
| "不想学了" | 温和接受，不劝说，调用 end_session |
| 连续 3 次说"不知道"/"不会" | 主动建议："要不要先休息一下？" |
| 沉默 10 秒+ | 由 `IdleTimeout` 事件触发（见 `AGENT-PROTOCOL.md` 事件表）："你还在吗？没关系的，我可以再说一遍" |

---

## 8. 与协议层的关系

本文档定义的规则在 Agent 的 `allowed_actions` 和阶段成功条件中体现：

- 降难规则 → 影响 `advance_phase` 调用时的阶段成功条件验证
- 优先级机制 → 影响 `<session_brief>` 中的 `today_focus` 和 `<name_plan>` 注入
- 路径选择 → 影响独立书写阶段的练习类型（写 vs 选）

---

## 9. 名字难字拆解规则

对应 `name_plan` 目标字中较难的字（笔画多、结构复杂），允许拆解教学。拆解只是教学过程，不改变姓名原文与目标字列表。

### 9.1 拆解条件（满足任一）

- 笔画数 ≥ 8 且独立书写连续 2 次失败
- 用户明确表示"这个字太难"或要求拆开学
- 在签字场景（签名练习）中卡住

### 9.2 拆解粒度

- 拆到"关键部件"为止（如 张 = 弓 + 长），不拆到单笔画，除非部件本身仍难
- 只教"能帮助记住整字"的部件，不教所有部件名称

### 9.3 拆解流程

`整体展示 → 拆部件分别认写 → 组合回整字 → 签名场景验证`

### 9.4 完成标准

以整字独立写出为准（不依赖部件提示），而非部件本身掌握；达标规则见 [`MASTERY-CRITERIA.md`](./MASTERY-CRITERIA.md) 签字达标标准。
